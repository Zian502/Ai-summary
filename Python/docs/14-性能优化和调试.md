# 性能优化和调试

## 性能分析

### timeit 模块

```python
import timeit

# 测量代码执行时间
time_taken = timeit.timeit('sum(range(100))', number=10000)
print(f"执行时间: {time_taken:.4f} 秒")
```

### cProfile 模块

```python
import cProfile

def slow_function():
    total = 0
    for i in range(1000000):
        total += i ** 2
    return total

# 性能分析
cProfile.run('slow_function()')
```

## 调试技巧

### pdb 调试器

```python
import pdb

def buggy_function(x, y):
    pdb.set_trace()  # 设置断点
    result = x / y
    return result

# 运行时会进入调试模式
# 常用命令：
# n (next): 下一行
# s (step): 进入函数
# c (continue): 继续执行
# p variable: 打印变量值
# q (quit): 退出
```

### 日志调试

```python
import logging

# 配置日志
logging.basicConfig(
    level=logging.DEBUG,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
)

logger = logging.getLogger(__name__)

def function():
    logger.debug("调试信息")
    logger.info("一般信息")
    logger.warning("警告信息")
    logger.error("错误信息")
```

## 性能优化技巧

### 使用生成器

```python
# 不好的做法：创建大列表
large_list = [x**2 for x in range(1000000)]

# 好的做法：使用生成器
large_gen = (x**2 for x in range(1000000))
```

### 使用内置函数

```python
# 不好的做法
total = 0
for num in numbers:
    total += num

# 好的做法
total = sum(numbers)
```

### 缓存结果

```python
from functools import lru_cache

@lru_cache(maxsize=128)
def expensive_function(n):
    # 昂贵的计算
    return sum(i**2 for i in range(n))
```

## 实际应用场景 Demo

### Demo 1: 性能对比

```python
#!/usr/bin/env python3
# performance_comparison.py - 性能对比

import timeit

def method1():
    """方法1：列表推导式"""
    return [x**2 for x in range(1000)]

def method2():
    """方法2：生成器表达式"""
    return list(x**2 for x in range(1000))

def method3():
    """方法3：循环"""
    result = []
    for x in range(1000):
        result.append(x**2)
    return result

# 性能测试
print("方法1（列表推导式）:", timeit.timeit(method1, number=10000))
print("方法2（生成器表达式）:", timeit.timeit(method2, number=10000))
print("方法3（循环）:", timeit.timeit(method3, number=10000))
```

## 下一步

恭喜！你已经完成了 Python 从入门到进阶的学习。建议：
1. 实践项目开发
2. 阅读优秀的开源代码
3. 参与 Python 社区

继续学习，不断进步！🎉

