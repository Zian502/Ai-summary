# 控制结构

## 条件语句（if/elif/else）

### 基本语法

```python
# 基本 if 语句
if condition:
    # 代码块
    pass

# if-else 语句
if condition:
    # 代码块1
    pass
else:
    # 代码块2
    pass

# if-elif-else 语句
if condition1:
    # 代码块1
    pass
elif condition2:
    # 代码块2
    pass
else:
    # 代码块3
    pass
```

### 条件表达式

```python
# 比较运算符
x = 10
print(x == 10)   # True（等于）
print(x != 10)   # False（不等于）
print(x > 5)     # True（大于）
print(x < 20)    # True（小于）
print(x >= 10)   # True（大于等于）
print(x <= 10)   # True（小于等于）

# 逻辑运算符
print(True and False)   # False（逻辑与）
print(True or False)    # True（逻辑或）
print(not True)         # False（逻辑非）

# 成员运算符
numbers = [1, 2, 3]
print(2 in numbers)     # True（在序列中）
print(4 not in numbers) # True（不在序列中）

# 身份运算符
a = [1, 2, 3]
b = [1, 2, 3]
c = a
print(a is b)           # False（不是同一个对象）
print(a is c)           # True（是同一个对象）
print(a == b)           # True（值相等）
```

### 三元运算符（条件表达式）

```python
# 基本语法：value_if_true if condition else value_if_false
x = 10
result = "正数" if x > 0 else "非正数"
print(result)  # 正数

# 嵌套三元运算符
x = 0
result = "正数" if x > 0 else ("零" if x == 0 else "负数")
print(result)  # 零
```

### 与其他语言对比

| 语言 | if 语句 | 三元运算符 |
|------|---------|-----------|
| **Python** | `if x > 0: pass` | `x if x > 0 else 0` |
| **Java** | `if (x > 0) { }` | `x > 0 ? x : 0` |
| **C++** | `if (x > 0) { }` | `x > 0 ? x : 0` |
| **JavaScript** | `if (x > 0) { }` | `x > 0 ? x : 0` |

**Python 特点**：
- 不需要括号（除非复杂表达式）
- 使用冒号和缩进表示代码块
- 使用 `elif` 而不是 `else if`
- 三元运算符语法独特

## 循环语句

### for 循环

```python
# 遍历序列
fruits = ["apple", "banana", "orange"]
for fruit in fruits:
    print(fruit)

# 遍历字符串
for char in "Python":
    print(char)

# 使用 range() 函数
for i in range(5):        # 0 到 4
    print(i)

for i in range(1, 6):     # 1 到 5
    print(i)

for i in range(0, 10, 2): # 0 到 9，步长为 2
    print(i)

# 遍历字典
person = {"name": "Python", "age": 30}
for key in person:
    print(key, person[key])

for key, value in person.items():
    print(key, value)

# 使用 enumerate() 获取索引
for index, fruit in enumerate(fruits):
    print(index, fruit)

# 使用 zip() 同时遍历多个序列
names = ["Alice", "Bob", "Charlie"]
ages = [25, 30, 35]
for name, age in zip(names, ages):
    print(name, age)
```

### while 循环

```python
# 基本 while 循环
count = 0
while count < 5:
    print(count)
    count += 1

# 无限循环（需要 break 退出）
while True:
    user_input = input("输入 'quit' 退出: ")
    if user_input == 'quit':
        break

# while-else 语句
count = 0
while count < 5:
    print(count)
    count += 1
else:
    print("循环正常结束")  # 如果循环没有被 break 中断，会执行
```

### 循环控制语句

```python
# break：跳出循环
for i in range(10):
    if i == 5:
        break  # 当 i 等于 5 时跳出循环
    print(i)   # 输出 0 到 4

# continue：跳过当前迭代
for i in range(10):
    if i % 2 == 0:
        continue  # 跳过偶数
    print(i)      # 只输出奇数：1, 3, 5, 7, 9

# pass：占位符，什么都不做
for i in range(10):
    if i == 5:
        pass  # 什么都不做，继续执行
    print(i)
```

### 与其他语言对比

| 语言 | for 循环 | while 循环 |
|------|---------|-----------|
| **Python** | `for x in iterable:` | `while condition:` |
| **Java** | `for (int i = 0; i < 10; i++)` | `while (condition) { }` |
| **C++** | `for (int i = 0; i < 10; i++)` | `while (condition) { }` |
| **JavaScript** | `for (let x of arr)` | `while (condition) { }` |

**Python 特点**：
- `for` 循环基于迭代器，不是传统的 C 风格
- 使用 `range()` 生成数字序列
- 支持 `for-else` 和 `while-else`

## 实际应用场景 Demo

### Demo 1: 猜数字游戏

```python
#!/usr/bin/env python3
# guess_number.py - 猜数字游戏

import random

def guess_number_game():
    """猜数字游戏"""
    # 生成随机数（1 到 100）
    secret_number = random.randint(1, 100)
    max_attempts = 7
    attempts = 0
    
    print("=" * 50)
    print("欢迎来到猜数字游戏！")
    print("=" * 50)
    print(f"我已经想好了一个 1 到 100 之间的数字")
    print(f"你有 {max_attempts} 次机会猜中它")
    print("=" * 50)
    
    while attempts < max_attempts:
        attempts += 1
        remaining = max_attempts - attempts + 1
        
        try:
            # 获取用户输入
            guess = int(input(f"\n第 {attempts} 次尝试（还剩 {remaining} 次）: "))
            
            # 验证输入范围
            if guess < 1 or guess > 100:
                print("请输入 1 到 100 之间的数字！")
                attempts -= 1  # 不消耗尝试次数
                continue
            
            # 判断猜测结果
            if guess == secret_number:
                print(f"\n🎉 恭喜你！你用了 {attempts} 次猜中了数字 {secret_number}！")
                break
            elif guess < secret_number:
                print(f"太小了！提示：数字比 {guess} 大")
            else:
                print(f"太大了！提示：数字比 {guess} 小")
                
        except ValueError:
            print("请输入有效的数字！")
            attempts -= 1  # 不消耗尝试次数
        except KeyboardInterrupt:
            print("\n\n游戏被中断，再见！")
            return
    
    # 如果循环正常结束（没有 break），说明没猜中
    else:
        print(f"\n很遗憾，你没有猜中。正确答案是 {secret_number}")

if __name__ == "__main__":
    guess_number_game()
```

**运行示例**：
```bash
python guess_number.py
# ==================================================
# 欢迎来到猜数字游戏！
# ==================================================
# 我已经想好了一个 1 到 100 之间的数字
# 你有 7 次机会猜中它
# ==================================================
# 
# 第 1 次尝试（还剩 7 次）: 50
# 太小了！提示：数字比 50 大
# ...
```

### Demo 2: 成绩等级判定系统

```python
#!/usr/bin/env python3
# grade_system.py - 成绩等级判定系统

def get_grade(score):
    """
    根据分数返回等级
    
    参数:
        score: 分数（0-100）
    
    返回:
        等级字符串
    """
    if score < 0 or score > 100:
        return "无效分数"
    elif score >= 90:
        return "A（优秀）"
    elif score >= 80:
        return "B（良好）"
    elif score >= 70:
        return "C（中等）"
    elif score >= 60:
        return "D（及格）"
    else:
        return "F（不及格）"

def process_students(students):
    """
    处理学生成绩列表
    
    参数:
        students: 学生信息列表，每个元素是 (姓名, 分数) 元组
    """
    print("=" * 60)
    print("成绩等级判定系统")
    print("=" * 60)
    
    # 统计各等级人数
    grade_count = {'A': 0, 'B': 0, 'C': 0, 'D': 0, 'F': 0}
    
    # 处理每个学生
    for name, score in students:
        grade = get_grade(score)
        print(f"{name:10s} 分数: {score:3d}  等级: {grade}")
        
        # 统计等级
        if grade.startswith('A'):
            grade_count['A'] += 1
        elif grade.startswith('B'):
            grade_count['B'] += 1
        elif grade.startswith('C'):
            grade_count['C'] += 1
        elif grade.startswith('D'):
            grade_count['D'] += 1
        elif grade.startswith('F'):
            grade_count['F'] += 1
    
    # 显示统计信息
    print("\n" + "=" * 60)
    print("等级统计")
    print("=" * 60)
    for grade, count in grade_count.items():
        if count > 0:
            print(f"{grade} 等级: {count} 人")

def main():
    """主函数"""
    # 学生成绩数据
    students = [
        ("张三", 95),
        ("李四", 85),
        ("王五", 75),
        ("赵六", 65),
        ("钱七", 55),
        ("孙八", 92),
        ("周九", 88),
        ("吴十", 78),
    ]
    
    process_students(students)
    
    # 计算平均分
    total_score = sum(score for _, score in students)
    average = total_score / len(students)
    print(f"\n平均分: {average:.2f}")

if __name__ == "__main__":
    main()
```

### Demo 3: 数据过滤和处理工具

```python
#!/usr/bin/env python3
# data_filter.py - 数据过滤和处理工具

def filter_and_process(numbers, min_value=None, max_value=None, even_only=False, odd_only=False):
    """
    过滤和处理数字列表
    
    参数:
        numbers: 数字列表
        min_value: 最小值（可选）
        max_value: 最大值（可选）
        even_only: 只要偶数
        odd_only: 只要奇数
    
    返回:
        过滤后的列表
    """
    result = []
    
    for num in numbers:
        # 检查最小值
        if min_value is not None and num < min_value:
            continue
        
        # 检查最大值
        if max_value is not None and num > max_value:
            continue
        
        # 检查奇偶性
        if even_only and num % 2 != 0:
            continue
        
        if odd_only and num % 2 == 0:
            continue
        
        # 通过所有过滤条件
        result.append(num)
    
    return result

def main():
    """主函数"""
    # 示例数据
    numbers = list(range(1, 21))  # [1, 2, 3, ..., 20]
    
    print("=" * 60)
    print("数据过滤和处理工具")
    print("=" * 60)
    
    print(f"\n原始数据: {numbers}")
    
    # 不同的过滤条件
    filters = [
        ("所有数据", {}),
        ("10 到 15 之间", {"min_value": 10, "max_value": 15}),
        ("只保留偶数", {"even_only": True}),
        ("只保留奇数", {"odd_only": True}),
        ("10 到 20 之间的偶数", {"min_value": 10, "max_value": 20, "even_only": True}),
    ]
    
    for description, kwargs in filters:
        filtered = filter_and_process(numbers, **kwargs)
        print(f"\n{description}: {filtered}")
        print(f"  数量: {len(filtered)}")
    
    # 使用列表推导式实现相同功能（更 Pythonic）
    print("\n" + "=" * 60)
    print("使用列表推导式实现")
    print("=" * 60)
    
    # 10 到 20 之间的偶数
    filtered = [x for x in numbers if 10 <= x <= 20 and x % 2 == 0]
    print(f"10 到 20 之间的偶数: {filtered}")

if __name__ == "__main__":
    main()
```

### Demo 4: 菜单驱动的程序

```python
#!/usr/bin/env python3
# menu_driven.py - 菜单驱动的程序

def show_menu():
    """显示菜单"""
    print("\n" + "=" * 50)
    print("菜单选项")
    print("=" * 50)
    print("1. 计算两数之和")
    print("2. 计算两数之积")
    print("3. 判断数字奇偶性")
    print("4. 打印数字序列")
    print("0. 退出程序")
    print("=" * 50)

def add_numbers():
    """计算两数之和"""
    try:
        a = float(input("请输入第一个数字: "))
        b = float(input("请输入第二个数字: "))
        result = a + b
        print(f"结果: {a} + {b} = {result}")
    except ValueError:
        print("错误: 请输入有效的数字")

def multiply_numbers():
    """计算两数之积"""
    try:
        a = float(input("请输入第一个数字: "))
        b = float(input("请输入第二个数字: "))
        result = a * b
        print(f"结果: {a} × {b} = {result}")
    except ValueError:
        print("错误: 请输入有效的数字")

def check_odd_even():
    """判断数字奇偶性"""
    try:
        num = int(input("请输入一个整数: "))
        if num % 2 == 0:
            print(f"{num} 是偶数")
        else:
            print(f"{num} 是奇数")
    except ValueError:
        print("错误: 请输入有效的整数")

def print_sequence():
    """打印数字序列"""
    try:
        start = int(input("请输入起始数字: "))
        end = int(input("请输入结束数字: "))
        step = int(input("请输入步长（默认1）: ") or "1")
        
        print(f"数字序列: ", end="")
        for i in range(start, end + 1, step):
            print(i, end=" ")
        print()
    except ValueError:
        print("错误: 请输入有效的整数")

def main():
    """主函数"""
    print("欢迎使用菜单驱动程序！")
    
    while True:
        show_menu()
        choice = input("\n请选择操作（输入数字）: ").strip()
        
        if choice == '0':
            print("感谢使用，再见！")
            break
        elif choice == '1':
            add_numbers()
        elif choice == '2':
            multiply_numbers()
        elif choice == '3':
            check_odd_even()
        elif choice == '4':
            print_sequence()
        else:
            print("无效的选择，请重新输入")
        
        # 等待用户按键继续
        input("\n按 Enter 键继续...")

if __name__ == "__main__":
    main()
```

## 嵌套循环

```python
# 嵌套 for 循环：打印乘法表
for i in range(1, 10):
    for j in range(1, i + 1):
        print(f"{j}×{i}={i*j}", end="\t")
    print()  # 换行

# 嵌套循环：查找二维列表中的元素
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
target = 5
found = False

for row_idx, row in enumerate(matrix):
    for col_idx, value in enumerate(row):
        if value == target:
            print(f"找到 {target} 在位置 ({row_idx}, {col_idx})")
            found = True
            break  # 跳出内层循环
    if found:
        break  # 跳出外层循环
```

## 下一步

完成控制结构学习后，建议学习：
1. [函数](./05-函数.md) - 函数定义、参数、返回值
2. [面向对象编程](./06-面向对象编程.md) - 类、对象、继承

